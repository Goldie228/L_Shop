/**
 * Конфигурация Jest для фронтенда L_Shop
 * 
 * Использует jsdom для симуляции браузерного окружения.
 * Это позволяет тестировать DOM-компоненты без реального браузера.
 * 
 * Документация: https://jestjs.io/docs/configuration
 */
module.exports = {
  // ============================================
  // Пресет
  // ============================================
  preset: 'ts-jest',

  // ============================================
  // Окружение тестирования
  // ============================================
  // jsdom - симуляция браузера (DOM API, fetch, localStorage)
  testEnvironment: 'jsdom',

  // ============================================
  // Корневые директории для поиска тестов
  // ============================================
  roots: ['<rootDir>/src/frontend'],

  // ============================================
  // Паттерны для поиска тестовых файлов
  // ============================================
  testMatch: ['**/__tests__/**/*.test.ts'],

  // ============================================
  // Маппинг модулей
  // ============================================
  // Для разрешения импортов без расширений
  moduleFileExtensions: ['ts', 'js', 'json'],

  // ============================================
  // Настройки покрытия кода (coverage)
  // ============================================
  collectCoverageFrom: [
    'src/frontend/**/*.ts',
    '!src/frontend/**/*.d.ts',
    '!src/frontend/**/__tests__/**',
    '!src/frontend/app.ts',
    '!src/frontend/vite-env.d.ts',
  ],

  // ============================================
  // Директория для отчётов покрытия
  // ============================================
  coverageDirectory: 'coverage/frontend',

  // ============================================
  // Форматы отчётов покрытия
  // ============================================
  coverageReporters: ['text', 'lcov', 'html'],

  // ============================================
  // Глобальные переменные
  // ============================================
  globals: {
    'ts-jest': {
      tsconfig: 'src/frontend/tsconfig.json',
    },
  },

  // ============================================
  // Моки для браузерных API
  // ============================================
  setupFilesAfterEnv: ['<rootDir>/src/frontend/__tests__/setup.ts'],

  // ============================================
  // Прочие настройки
  // ============================================
  verbose: true,
  testTimeout: 10000,
  clearMocks: true,
  resetMocks: true,
  restoreMocks: true,
};